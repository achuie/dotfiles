# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Fira Code Custom 6

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec wezterm

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
set $menu tofi-run -c $HOME/.config/tofi/tofi_run_theme | xargs swaymsg exec --
bindsym $mod+d exec --no-startup-id $menu

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# toggle sticky (only for floating windows)
bindsym $mod+g sticky toggle

# split in horizontal orientation
bindsym $mod+b split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+c focus child

# dynamic tagging
bindsym $mod+x exec --no-startup-id $HOME/.config/sway/dyn_tags.sh
bindsym $mod+Shift+x exec --no-startup-id $HOME/.config/sway/dyn_tags.sh move

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# move focused container to scratchpad
bindsym $mod+Mod1+Shift+1 mark "alpha", move scratchpad
bindsym $mod+Mod1+Shift+2 mark "beta", move scratchpad
bindsym $mod+Mod1+Shift+3 mark "gamma", move scratchpad
bindsym $mod+Mod1+Shift+4 mark "delta", move scratchpad
bindsym $mod+Mod1+Shift+5 mark "epsilon", move scratchpad
bindsym $mod+Mod1+Shift+6 mark "zeta", move scratchpad
bindsym $mod+Mod1+Shift+7 mark "eta", move scratchpad
bindsym $mod+Mod1+Shift+8 mark "theta", move scratchpad
bindsym $mod+Mod1+Shift+9 mark "iota", move scratchpad
bindsym $mod+Mod1+Shift+0 mark "kappa", move scratchpad

# toggle show container from scratchpad
bindsym $mod+Mod1+1 [con_mark="alpha"] scratchpad show
bindsym $mod+Mod1+2 [con_mark="beta"] scratchpad show
bindsym $mod+Mod1+3 [con_mark="gamma"] scratchpad show
bindsym $mod+Mod1+4 [con_mark="delta"] scratchpad show
bindsym $mod+Mod1+5 [con_mark="epsilon"] scratchpad show
bindsym $mod+Mod1+6 [con_mark="zeta"] scratchpad show
bindsym $mod+Mod1+7 [con_mark="eta"] scratchpad show
bindsym $mod+Mod1+8 [con_mark="theta"] scratchpad show
bindsym $mod+Mod1+9 [con_mark="iota"] scratchpad show
bindsym $mod+Mod1+0 [con_mark="kappa"] scratchpad show

# move workspace between outputs
bindsym $mod+Control+Mod1+Right move workspace to output right
bindsym $mod+Control+Mod1+Left move workspace to output left

bindsym $mod+Control+Mod1+l move workspace to output right
bindsym $mod+Control+Mod1+h move workspace to output left

# use left/right to rotate through workspaces; add Shift to move windows
bindsym $mod+Control+Left workspace prev
bindsym $mod+Control+Right workspace next
bindsym $mod+Control+Shift+Left move workspace prev
bindsym $mod+Control+Shift+Right move workspace next

bindsym $mod+Control+h workspace prev
bindsym $mod+Control+l workspace next
bindsym $mod+Control+Shift+h move workspace prev
bindsym $mod+Control+Shift+l move workspace next

# back and forth when switching away from current workspace
workspace_auto_back_and_forth yes

# reload the configuration file
bindsym $mod+Shift+c reload

# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "swaynag -t warning -m 'You pressed the exit \
shortcut. Do you really want to exit sway? This will end your Wayland session.' -b \
'Yes, exit sway' 'swaymsg exit'"

# lock the screen
bindsym $mod+o exec hyprlock

# resize window (you can also use the mouse for that)
mode "resize" {
    # These bindings trigger as soon as you enter the resize mode

    # Pressing left will shrink the window’s width.
    # Pressing right will grow the window’s width.
    # Pressing up will shrink the window’s height.
    # Pressing down will grow the window’s height.
    bindsym h resize shrink width 5 px or 5 ppt
    bindsym j resize grow height 5 px or 5 ppt
    bindsym k resize shrink height 5 px or 5 ppt
    bindsym l resize grow width 5 px or 5 ppt

    # same bindings, but for the arrow keys
    bindsym Left resize shrink width 5 px or 5 ppt
    bindsym Down resize grow height 5 px or 5 ppt
    bindsym Up resize shrink height 5 px or 5 ppt
    bindsym Right resize grow width 5 px or 5 ppt

    # Resize and move to corner
    bindsym c floating enable, resize set 20 ppt 20 ppt, \
        exec --no-startup-id $HOME/.config/sway/move_screen_ppt.sh 79 77

    bindsym s move position center, resize set width 50 ppt height 70 ppt

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

mode "music" {
    # Bindings to control mpd via mpc
    bindsym Return       exec mpc play
    bindsym s            exec mpc stop
    bindsym p            exec mpc toggle
    bindsym Shift+period exec mpc next
    bindsym Shift+comma  exec mpc prev
    bindsym z            exec mpc random
    bindsym y            exec mpc single
    bindsym r            exec mpc repeat
    bindsym plus         exec mpc volume +5
    bindsym minus        exec mpc volume -5

    bindsym Escape mode "default"
}

bindsym $mod+m mode "music"

# Window rules
for_window [app_id="firefox"] move --no-auto-back-and-forth window to workspace 1:web
for_window [app_id="discord"] move --no-auto-back-and-forth window to workspace 10:discord
for_window [app_id="wezterm-system"] mark "alpha", move scratchpad
for_window [app_id="wezterm-audio"] mark "beta", move scratchpad

# for_window [app_id=".*wezterm.*"] blur enable
# for_window [floating=true] shadows enable

for_window [shell="xwayland"] title_format "[XWayland] %title"

# Startup services and programs
exec --no-startup-id wezterm start --class "wezterm-system"
exec --no-startup-id wezterm start --class "wezterm-audio" -- zsh -is eval 'pulsemixer'

exec_always --no-startup-id dunst
exec_always --no-startup-id fcitx5
exec_always --no-startup-id swayidle -w \
    timeout 15 'if pgrep -x hyprlock; then swaymsg "output * power off"; fi' \
    resume 'swaymsg "output * power on"'

# Styling
client.urgent #2f343a #900000 #ffffff #900000

titlebar_padding 2

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    font pango:Fira Code Custom, Font Awesome 7 Free 6
    status_command i3status-rs $HOME/.config/i3status-rs/config.toml
    status_padding 0

    icon_theme Adwaita

    # Handle urgency hints
    colors {
        urgent_workspace   #2f343a #900000 #ffffff
    }
}

# Wayland input
input "1:1:AT_Translated_Set_2_keyboard" xkb_options "ctrl:swapcaps"
input "2362:628:PIXA3854:00_093A:0274_Touchpad" {
    tap enabled
    scroll_factor 0.3
    dwt disabled
}

seat * xcursor_theme "Adwaita" 1

# ICC color profile; requires vulkan wlr-renderer
output eDP-1 color_profile icc $HOME/.config/icc-profiles/framework-13-11thgen/BOE_CQ_______NE135FBM_N41_01.icm

# Wallpaper
output * bg $HOME/.background_image.jpg fill

# Use included Arch package config for systemd to start xdg-desktop-portal as needed
include /etc/sway/config.d/*
